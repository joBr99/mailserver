user = vmail
password = %vmaildbpassplaceholder%
hosts = %hostnameplaceholder%
dbname = vmail
query = select 1 as found from accounts where username = '%u' and domain = '%d' and enabled = true LIMIT 1;
